export const metadata = {
  title: "How To Center a Div",
  publishDate: "2024-01-09T00:00:00Z",
  categories: ["CSS"],
  interactive: true,

shortDescription:
"Back in the day, centering an element was one of the trickiest things in CSS. As the language has evolved, we have been given lots of new tools we can use… But how do we pick the best option? When do we use Flexbox, or CSS Grid, or something else? Let's dig into it.",
}

import Custom from "./components/custom"

# How to Center a Div in CSS

Centering a div in CSS is a common task for web developers. There are multiple methods to achieve this, depending on whether you want to center the div horizontally, vertically, or both. This guide will walk you through various techniques to center a div using CSS.

<Custom />

## Centering Horizontally

### Using `margin: auto`

The `margin: auto` method is a straightforward way to center a block-level element horizontally.

```css
.container {
  width: 50%;
  margin: 0 auto;
  background-color: lightblue;
}
```
